apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-traefik
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  traefik.json: |-
{{ .Files.Get "files/dashboards/traefik.json" | indent 4 }}
