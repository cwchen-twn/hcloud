apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-k3s
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  k3s.json: |-
{{ .Files.Get "files/dashboards/k3s.json" | indent 4 }}
