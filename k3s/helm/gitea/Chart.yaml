apiVersion: v2
name: gitea
description: Gitea Git service with external PostgreSQL, cert-manager certificate, and Prometheus metrics
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: gitea
    version: "^12.0.0"
    repository: "https://dl.gitea.com/charts/"
